before_install:
  # Addresses known issue on Travis CI w/ `ruby-2.5.x`
  #
  # Further details at the link below:
  #
  # https://github.com/travis-ci/travis-ci/issues/8978
  - gem update --system

language: ruby

rvm:
  - 2.3.6
  - 2.4.3
  - 2.5.1
